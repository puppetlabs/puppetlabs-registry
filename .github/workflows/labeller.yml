name: community-labeller

on:
  workflow_dispatch:

jobs:
  label:
    runs-on: ubuntu-latest
    steps:

      - uses: puppetlabs/community-labeller@test
        name: Label issues or pull requests
        with:
          label_name: community
          label_color: '5319e7'
          org_membership: puppetlabs
          fail_if_member: 'true'
          token: ${{ secrets.IAC_COMMUNITY_LABELER }}
